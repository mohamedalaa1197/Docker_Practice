services:
  node-app: 
    build:
      context: .
      args:
        - NODE_ENV=development
    volumes:
      - ./src:/app/src:ro
    environment:
      - NODE_env=development
    command: npm run start-dev
